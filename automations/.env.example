# ==============================================================================
# ENVIRONMENT VARIABLES FOR MODAL AUTOMATIONS
# Copy this file to '.env' and fill in production values, then sync to:
#   modal secret create evo-logistics-env --from-dotenv automations/.env --force
# ==============================================================================

# OpenAI keys
OPENAI_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxx"
MODAL_LLM_API_KEY="modalresearch_..."

# Supabase
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOi..."

# Gmail + Pub/Sub
GOOGLE_PUBSUB_TOPIC="projects/your-project-id/topics/gmail-push-notifications"
GOOGLE_OAUTH_CLIENT_ID="your-google-oauth-client-id.apps.googleusercontent.com"
GOOGLE_OAUTH_CLIENT_SECRET="your-google-oauth-client-secret"
MAILBOX_TOKEN_ENCRYPTION_KEY="32-byte-key-as-base64url-base64-hex-or-raw"

# Workspace routing controls
BOOTSTRAP_WORKSPACE_ID="00000000-0000-0000-0000-000000000001"
ALLOW_BOOTSTRAP_WORKSPACE_FALLBACK="false"

# ------------------------------------------------------------------------------
# Phase 4 — Land Freight API Integration (freight_apis/)
# Providers run in mock mode until FREIGHT_API_MODE=live AND the matching
# credentials below are set. Leave blank to stay in mock mode.
# ------------------------------------------------------------------------------
FREIGHT_API_MODE="mock"             # mock | live
DAT_API_KEY=""                       # DAT rate intelligence (FTL)
SMC3_USERNAME=""                     # SMC3 LTL rating
SMC3_PASSWORD=""
UBER_FREIGHT_API_KEY=""              # Uber Freight FTL instant quote
LOADSMART_API_KEY=""                 # Loadsmart FTL instant quote
VUCEM_RFC=""                         # VUCEM cross-border (Mexico) — scaffold
VUCEM_API_KEY=""

# Detention/demurrage: set to "true" to email an internal alert to the workspace
# mailbox when a tracked event starts accruing (default off = accrual + log only).
DD_ALERT_EMAILS="false"
